import React from 'react';

function Settings() {
    return (
        <div>
            All settings managed on this page. Things like dark mode or changing the layout of pages perhaps.
        </div>
    );
};

export default Settings;